name: Build OS64

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4
    - name: Step 1
      run: sudo apt install nasm
    - name: Step 2
      run: make
  
    
